<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.wheelmap.android.ui"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="4" />
    
    <uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application android:icon="@drawable/ic_launcher" android:label="@string/app_name">
        <uses-library android:name="com.google.android.maps" />
        
        
        
        <activity android:name=".WheelmapHomeActivity"
                  android:theme="@style/Theme.IOSched"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".POIsListActivity"
            android:theme="@style/Theme.IOSched"
            android:label="@string/title_pois_list" />
            
        <activity
            android:name=".POIsMapActivity"
            android:theme="@style/Theme.IOSched"
            android:label="@string/title_pois_map" />
        <activity
            android:name=".POIDetailActivity"
            android:theme="@style/Theme.IOSched"
            android:label="@string/title_pois_detail" />
            
            
            
            

    </application>
</manifest>