// Preliminaries:
Das android-maven Modul benötigt unbedingt die Maven-Version > 3.0.3


APKLib-Dependencies können nicht automatisch von Eclipse eingebunden werden, sodass
Eclipse rote Fehler anzeigt. Deshalb ist es nötig, diese Projekte manuell aus Git 
auszuchecken und im Eclipse wheelmap Subprojekt diese Libs manuell in ADT einzutragen .

APKLib Dependencies sind
https://github.com/JakeWharton/ActionBarSherlock.git
https://github.com/chrisbanes/Android-PullToRefresh.git
https://github.com/ChristopheVersieux/HoloEverywhere.git

Und als Maven-Project in Eclipse zu importieren und in der Projektkonfiguration von 
wheelmap im Android-Tab diese Projekte als Library-Projekte einzutragen.

Wenn es einen Fehler beim Importieren des Maven Projekts in Eclipse gibt, erzeuge die Files
wheelmap/project.properties
wheelmap-it/project.properties
von Hand und importiere die Projekte nochmals

// Ein update der Copyright Header erfolgt mit
mvn license:update-file-header

// Ein update der Third-Party License Header
mvn license:add-third-party -DuseMissingFile

// Maven commands:
mvn compile, mvn install 
- compiliert oder baut

mvn android:deploy 
- im wheelmap Subprojekt installiert auf einem verfügbaren Gerät

mvn android:run
- im wheelmap Subprojekt führt das Projekt auf einem verfügbaren Gerät aus

mvn integration-test
- im wheelmap-it fuehrt die Instrumentation-Tests aus

Ein unsigniertes APK findet sich unter
wheelmap/target/wheelmap-1.0-SNAPSHOT.ap_


